joins will merge two tables and form a big row as a result,which columns we r selecting will be displayed is called as inner joins
SELECT EMP.EMP_NAME,EMP.EMP_AGE,EMP.EMP_SALARY,STU.NAME,STU.SUBJECT FROM EMP INNER JOIN STU ON EMP.ID=STU.ID;
SELECT EMP.EMP_NAME,EMP.EMP_AGE,EMP.EMP_SALARY,STU.NAME,STU.SUBJECT FROM EMP LEFT OUTER JOIN STU ON EMP.ID=STU.ID;
SELECT EMP.EMP_NAME,EMP.EMP_AGE,EMP.EMP_SALARY,STU.NAME,STU.SUBJECT FROM EMP RIGHT OUTER JOIN STU ON EMP.ID=STU.ID;
